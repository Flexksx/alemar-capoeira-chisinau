set working-directory := "../.."

[private]
default:
    just --list --justfile {{source_file()}}

# Lint Markdown files
markdown:
    rumdl check .

# Lint the webapp (ESLint)
webapp:
    pnpm -C webapp lint:check

# Run every linter
all: markdown webapp
